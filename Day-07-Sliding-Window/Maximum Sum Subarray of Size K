arr = [2, 1, 5, 1, 3, 2]
k = 3

total = 0

# First window
for i in range(k):
    total += arr[i]

maximum = total

# Slide the window
for i in range(k, len(arr)):
    total = total - arr[i-k] + arr[i]

    if total > maximum:
        maximum = total

print("Maximum sum:", maximum)
